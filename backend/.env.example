# Copy to .env and add keys when you get them (e.g. on hackathon day).
# Until then the app runs in stub mode — no keys required.
MODULATE_API_KEY=
YUTORI_API_KEY=
FASTINO_API_KEY=
PIONEER_API_KEY=
# Neo4j (memory/profile store)
NEO4J_URI=
NEO4J_USERNAME=
NEO4J_PASSWORD=
# Optional (defaults to Neo4j's default database)
NEO4J_DATABASE=
# Optional: Tavily for fast factual lookups
# TAVILY_API_KEY=
